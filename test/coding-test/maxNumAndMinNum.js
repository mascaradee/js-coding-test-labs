const assert = require("assert");
const { log } = console;


function solution(n, m) {

  return [];

}


describe("최대공약수와 최소공배수", function () {
  it("테스트 케이스1 ", function () {
    assert.deepStrictEqual(solution(3, 12), [3, 12]);
  });
  it("테스트 케이스2 ", function () {
    assert.deepStrictEqual(solution(2, 5), [1, 10]);
  });
});
